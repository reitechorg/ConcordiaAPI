<html lang="en">

<head>
	<meta charset="UTF-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<title>{{server.name}} - Concordia</title>

	<link rel="preconnect" href="https://fonts.googleapis.com" />
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
	<link href="https://fonts.googleapis.com/css2?family=Funnel+Sans:ital,wght@0,300..800;1,300..800&display=swap"
		rel="stylesheet" />

	<style>
		body {
			padding: 0;
			margin: 0;
			font-family: 'Funnel Sans', sans-serif;
			color: white;
		}

		.background {
			display: flex;
			justify-content: center;
			align-items: center;
			height: 100vh;
			background: linear-gradient(315deg, #654ea3, #eaafc8);
		}

		.container {
			background-color: #0f0f0f;
			display: flex;
			flex-direction: column;
			gap: 1rem;
			border-radius: 1rem;
			max-width: 100%;
			min-width: fit-content;
			padding: 1.5rem;
		}

		.icon {
			width: 7rem;
			height: 7rem;
			border-radius: .5rem;
			object-fit: cover;
		}

		h1 {
			font-size: 2rem;
			margin: 0;
		}

		.info__welcome {
			font-size: 1rem;
			font-weight: 300;
			opacity: .9
		}

		.info__desciption {
			font-size: 1rem;
			font-weight: 300;
			opacity: .8;
			margin: .5rem 0
		}

		.join-btn {
			background-color: #654ea3;
			color: white;
			border: none;
			border-radius: .5rem;
			cursor: pointer;
			font: inherit;
			padding: .5rem .75rem;
			width: 100%;
		}

		.info__join {
			margin-top: 1rem;
		}

		.server {
			display: flex;
			gap: 1rem;
		}

		.download {
			font-size: .8rem;
			opacity: .7;
		}

		.download a {
			color: inherit;
		}

		.join-btn:hover {
			background-color: #5c4397;
		}

		.download a:hover {
			text-decoration: none;
		}
	</style>
</head>

<body>
	<div class="background">
		<main class="container">
			<div class="server">
				<img class="icon" src="{{server.iconUrl}}" alt="{{server.name}}" />
				<div class="info">
					<div class="info__welcome">You are welcome to join</div>
					<h1>{{server.name}}</h1>
					<p class="info__desciption">{{server.description}}</p>
				</div>
			</div>
			<button class="join-btn" id="js-join-btn">
				Join
				{{server.name}}
			</button>
			<div class="download">
				You need Concodia client to join this server.
				<br />
				You don't have it yet?
				<a href="https://reitech.org/creations/concordia" target="_blank">Download</a>
				it now!

			</div>
		</main>
	</div>

	<script>
		document.getElementById('js-join-btn').addEventListener('click', () => { window.location.href = 'concordia://join?url={{server.url}}'; });
	</script>
</body>

</html>